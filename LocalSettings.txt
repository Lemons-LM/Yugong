#TODO: Change this to a TOML

# Fill the items as need. If it is something you don't need, it is something you can leave it empty or just delete them.

# Wiki settings
api_endpoint=
user_agent=
is_owner_only= # If true, set your oauth access token in environment variables named MW_OAUTH_TOKEN
client_id=

# About Task
page_id_start=
page_id_end=
linked_template=
category=

# Settings/Behavior
safe_tag_list= # Change to a list of safe tags
max_acceptable_diff_size=
overwrite_para_name=
overwrite_template_name=
enable_cangjie=
zh_convert_to=
request_max_tries=
edit_comment=

# Other Settings in Extensions